# AirWatch clean-room harness (STUB)

TODO: port the whole harness from `pollenwatch/cleanroom/` ~as-is (it is
domain-agnostic). Files to bring: bootstrap.py, upgrade.py, verify.py, lint.py,
cleanup.py, lib/{ha_api,ha_ws,ha_flow,hacs,onboarding,snapshot}.py,
config/{pinned_release,matrix,log_allowlist}.json, assets/{hacs zip + lock}.
Adapt: domain=airwatch, baseline tag once v1 ships, log allowlist.
