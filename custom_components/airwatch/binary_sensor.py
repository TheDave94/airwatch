"""AirWatch — DivergenceSensor (binary flag when sources disagree > 1 band).

TODO: port from pollenwatch/.../binary_sensor.py ~AS-IS. Band definition comes
from the threshold tables instead of pollen levels.
"""
