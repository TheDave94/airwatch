"""AirWatch — pure cross-source analytics (consensus / divergence /
recent-percentile). No HA imports (testable).

TODO: port from pollenwatch/.../analytics.py ~AS-IS. The bucketing input is
pollutant bands (WHO/EU/US-AQI) rather than pollen levels; the consensus/
divergence math is identical.
"""
