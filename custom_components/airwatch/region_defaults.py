"""AirWatch — per-country/region pollutant preselection for onboarding.

TODO: REBUILD (not a port). PollenWatch preselects species by country; AirWatch
preselects which pollutants matter by region (e.g. NO2/PM near traffic, O3 in
summer-south). Open question: default pollutant set per region.
"""
